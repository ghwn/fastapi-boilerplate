version: "3"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - .:/code:z
    ports:
      - "8000:8000"
    working_dir: /code
    command: |
      sh -c "uvicorn app.main:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped
